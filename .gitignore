# Downloaded/generated data and simulation outputs - not source, don't commit.
# Scripts under BCI/R/, EDTS_run/*.sh, and docker/ ARE tracked (that's the
# whole point of self-containment: the reproducible code lives in git, the
# large/downloaded data does not).
BCI/raw_data/
BCI/run/
EDTS_run/common/
EDTS_run/umbs/
EDTS_run/test-outputs/
EDTS_run/ED2IN

# Copy of an external repo (ED2_Support_Files) - not this repo's own source.
# Re-fetch instead of committing; see README_PERSONAL.md.
ED2_Support_Files-master/

# Stray Python env from an earlier (now-removed) part of the pipeline.
.venv/
